---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: schemaversions.client.sroperator.io
spec:
  group: client.sroperator.io
  names:
    kind: SchemaVersion
    listKind: SchemaVersionList
    plural: schemaversions
    singular: schemaversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The schema subject
      jsonPath: .spec.subject
      name: Subject
      type: string
    - description: The schema version
      jsonPath: .spec.version
      name: Version
      type: integer
    - description: Whether the schema is active
      jsonPath: .status.active
      name: Active
      type: boolean
    - description: Whether the schema is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SchemaVersion is the Schema for the schemaversions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SchemaVersionSpec defines the desired state of SchemaVersion
            properties:
              content:
                description: Used to define the schema content
                type: string
              subject:
                description: Used to define the schema subject, concatenated with
                  the schema name and target
                type: string
              version:
                description: Used to define the schema version
                format: int32
                type: integer
            required:
            - content
            - subject
            - version
            type: object
          status:
            description: SchemaVersionStatus defines the observed state of SchemaVersion
            properties:
              active:
                description: Used to define whether the schema is active, i.e. the
                  latest version
                type: boolean
              ready:
                description: Used to define whether the schema is applied to the schema
                  registry
                type: boolean
            required:
            - active
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
